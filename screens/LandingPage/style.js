import { StyleSheet, Text, View } from 'react-native'


const styles = StyleSheet.create({
    buttonContainer: {
        position: 'absolute',
        bottom: 50,
        width: "100%"
    },
})

export default styles;

